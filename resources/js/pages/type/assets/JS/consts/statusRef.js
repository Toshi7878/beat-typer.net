import { ref } from 'vue';

//ステータス
export const score = ref(0)
export const point = ref(0)
export const missCount = ref(0)
export const lostCount = ref(0)
export const typeCount = ref(0)
export const typeSpeed = ref(0)
export const rank = ref(0)
export const lineCount = ref(0)